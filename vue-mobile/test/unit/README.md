# Files mobile unit tests (Vitest)

```bash
cd modules/FilesMobileWebclient/vue-mobile
yarn
yarn test:unit
```

Template mirrors Mail/Core: `test/unit/**/*.spec.js`, `yarn test:unit`, pre-commit when `vue-mobile/` is staged.
